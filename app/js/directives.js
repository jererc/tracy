'use strict';

(function() {

    var directives = angular.module('tracyDirectives', []);

    // directives.directive('errorsActions', function(utilsSvc) {
    //     return function(scope, element, attrs) {
    //         var actions = element.find(attrs.mediaActions);
    //         if (utilsSvc.mobile) {
    //             actions.fadeIn(100);
    //         } else {
    //             element.hover(function() {
    //                 actions.stop(true).fadeIn(50);
    //             }, function() {
    //                 actions.stop(true).fadeOut(50);
    //             });
    //         }
    //     };
    // });

})();
